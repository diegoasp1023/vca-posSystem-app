# Keycloak

Configuración de Keycloak (IAM/seguridad) para VCA POS. La documentación
completa — clients, roles, provisioning, comandos de arranque por ambiente y
endurecimiento — vive en [`docs/keycloak.md`](../docs/keycloak.md).

Esta carpeta solo contiene documentación por ahora — no crear subcarpetas
vacías (`themes/`, `providers/`, `scripts/`, `realm-export/`) de forma
anticipada (ver [`CLAUDE.md`](../CLAUDE.md)).
