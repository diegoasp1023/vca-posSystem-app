parent=keycloak.v2
import=common/keycloak

styles=css/styles.css css/vca-pos.css

# The site has no dark mode of its own (see apps/frontend) — keep the login
# a single committed light look instead of following the OS preference,
# so it doesn't clash with the light-only card/input colors in vca-pos.css.
darkMode=false
